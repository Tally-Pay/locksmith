/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { LOCKSMITH_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const TRANSFER_ADMIN_DISCRIMINATOR = 1;

export function getTransferAdminDiscriminatorBytes() {
  return getU8Encoder().encode(TRANSFER_ADMIN_DISCRIMINATOR);
}

export type TransferAdminInstruction<
  TProgram extends string = typeof LOCKSMITH_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountNewAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountNewAdmin extends string
        ? ReadonlyAccount<TAccountNewAdmin>
        : TAccountNewAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type TransferAdminInstructionData = { discriminator: number };

export type TransferAdminInstructionDataArgs = {};

export function getTransferAdminInstructionDataEncoder(): FixedSizeEncoder<TransferAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU8Encoder()]]),
    (value) => ({ ...value, discriminator: TRANSFER_ADMIN_DISCRIMINATOR })
  );
}

export function getTransferAdminInstructionDataDecoder(): FixedSizeDecoder<TransferAdminInstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getTransferAdminInstructionDataCodec(): FixedSizeCodec<
  TransferAdminInstructionDataArgs,
  TransferAdminInstructionData
> {
  return combineCodec(
    getTransferAdminInstructionDataEncoder(),
    getTransferAdminInstructionDataDecoder()
  );
}

export type TransferAdminInput<
  TAccountAdmin extends string = string,
  TAccountNewAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  /** Current admin */
  admin: TransactionSigner<TAccountAdmin>;
  /** New admin pubkey */
  newAdmin: Address<TAccountNewAdmin>;
  /** Config account */
  config: Address<TAccountConfig>;
};

export function getTransferAdminInstruction<
  TAccountAdmin extends string,
  TAccountNewAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof LOCKSMITH_PROGRAM_ADDRESS,
>(
  input: TransferAdminInput<TAccountAdmin, TAccountNewAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress }
): TransferAdminInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountNewAdmin,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOCKSMITH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    newAdmin: { value: input.newAdmin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.newAdmin),
      getAccountMeta(accounts.config),
    ],
    data: getTransferAdminInstructionDataEncoder().encode({}),
    programAddress,
  } as TransferAdminInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountNewAdmin,
    TAccountConfig
  >);
}

export type ParsedTransferAdminInstruction<
  TProgram extends string = typeof LOCKSMITH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Current admin */
    admin: TAccountMetas[0];
    /** New admin pubkey */
    newAdmin: TAccountMetas[1];
    /** Config account */
    config: TAccountMetas[2];
  };
  data: TransferAdminInstructionData;
};

export function parseTransferAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedTransferAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      newAdmin: getNextAccount(),
      config: getNextAccount(),
    },
    data: getTransferAdminInstructionDataDecoder().decode(instruction.data),
  };
}
